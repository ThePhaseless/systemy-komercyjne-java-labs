# Taskplane runtime artifacts (machine-specific, do not commit)
.pi/batch-state.json
.pi/batch-history.json
.pi/lane-state-*
.pi/merge-result-*
.pi/merge-request-*
.pi/worker-conversation-*
.pi/orch-logs/
.pi/orch-abort-signal
.pi/settings.json
.worktrees/
.taskplane-tasks/

# Pi project-local packages (if using pi install -l)
.pi/npm/

# Java / Maven build output
**/target/

# Windows metadata sidecars copied into WSL
*:Zone.Identifier

# Local Taskplane and source materials not meant for publishing
.pi/
taskplane-tasks/
Laboratoria 1.docx
Laboratoria 2.docx
